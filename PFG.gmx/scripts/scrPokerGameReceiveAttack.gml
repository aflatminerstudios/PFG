/// scrPokerGameReceiveAttack(gameInstance)

var gameInstance = argument0;

// stand-in function for receiving an attack
show_debug_message("Stand-in for non-existent receive attack method for PokerGame.");
